SELECT TILL_RECEIPT_NUMBER
FROM (SELECT TILL_RECEIPT_NUMBER, CUSTOMER_ID 
FROM transactionsT
GROUP BY TILL_RECEIPT_NUMBER, CUSTOMER_ID
)  AS [%$##@_Alias]
GROUP BY TILL_RECEIPT_NUMBER
HAVING COUNT(CUSTOMER_ID) >=2;